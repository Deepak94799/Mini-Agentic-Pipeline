# Mini-Agentic-Pipeline
A mini agentic pipeline that retrieves relevant context from a small KB, uses an LLM to reason and decide the next step, executes actions via tools (API/web search/CSV), and returns a final answer with a clear step-by-step trace.
